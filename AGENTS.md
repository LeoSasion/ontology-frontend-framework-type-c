# AIBI-C 仓库执行约束

本文件是仓库级强制规则。AIBI-C 是独立产品，不是其他 AIBI 项目的分支、部署变体、迁移目标或共享工作树。

## 身份守卫

- 仓库根目录：`C:\Users\Administrator\Documents\AIBI-C`
- Git 远端：`https://github.com/LeoSasion/AIBI-C.git`
- 本地 API：`127.0.0.1:8787`
- Vite UI：`127.0.0.1:8686`

在读取、编辑、测试、提交、启动或停止本项目之前，必须执行：

```powershell
git rev-parse --show-toplevel
git remote get-url origin
```

结果必须明确指向上述 AIBI-C 根目录和远端；任何不一致都必须立即停止并报告。

## AIBI 系列隔离

- `AIBI-A`、`AIBI-B`、`AIBI-C`、`AIBI-D`、`AIBI-E` 是五个相互独立的产品和 Git 仓库；后缀表示身份，不表示版本或分支。
- 只允许只读借鉴其他项目的问题拆解、产品思路、交互策略和通用架构原则。
- 禁止跨仓库复制、导入、同步、移植或共享代码、文档、环境文件、数据库、测试、Fixtures、端口配置和运行回执。
- 借鉴结论必须按 AIBI-C 自身需求、合同、技术栈和验收标准重新设计、独立实现。
- 旧名称 `AIBI项目杂交` 只可作为隔离阻断词存在，不得成为运行路径、依赖或回退来源。

## 执行边界

- 只操作 AIBI-C 的文件、进程、端口、数据库、缓存、Secrets、日志、构建产物和 Git 状态。
- 不启动、停止、清理、测试、提交或修复其他 AIBI 仓库。
- 发现跨仓库绝对路径、符号链接、共享数据库、共享运行状态或外部 AIBI 测试输入时，停止并报告，不得自行绕过。
- 保留用户已有未提交改动；不得用 `reset --hard`、`checkout --` 或同类破坏性方式清理工作树。
- 写入类能力必须保留预演、显式确认、工作区隔离和证据回执；只读操作不得暗含业务写入。

## 文档与证据

- 文档入口为 `docs/README.md`，同一事实只在一个职责文件维护。
- 当前实现以代码、`package.json`、运行时和最新自动化回执为准；历史审计不得覆盖当前事实。
- 用户数据、凭据、真实数据库、业务导出和临时测试产物不得进入仓库。
- 验收证据只能证明对应日期与范围；当前状态统一写入 `docs/implementation-status.md`。
