# AIBI-C Product Positioning

## One-Sentence Positioning

AIBI-C 是面向本地业务导出表的证据型 AI BI 工作台：用户导入真实数据并说出想看的业务问题，系统优先生成一个可追溯、可审阅、确认后才写入的图表或答案。

## Product Category

AIBI-C 属于 AI-assisted BI workbench，而不是传统 BI 配置器、固定行业模板库或无证据聊天机器人。

- AI 负责理解意图、缩短配置路径和解释结果。
- 确定性运行时负责导入、语义、查询、证据和写入边界。
- 用户负责确认不确定字段和真实写入。
- 当前交付形态是单用户、本地优先的桌面工作台；不是云端多人 BI 服务。

## Primary Users And Job

主要用户是运营、财务、供应链、电商和 ERP 使用者。他们通常有 CSV、XLSX 或 XLSM 导出文件，知道业务问题，但不想先学习 SQL、数据建模和复杂看板编辑。

核心任务是：从一份真实业务数据出发，用最少步骤得到第一个可信图表，并能回答“数据来自哪里、使用了什么口径、哪些内容还不确定”。

数据实施和分析人员是次要用户。他们使用高级区域维护字段语义、指标、公式、关系、视图和看板，但这些能力不应占据新手第一屏。

## Value Proposition

1. 更快得到结果：导入后直接描述问题，稳定默认是一次对话生成一个图表草案。
2. 不隐藏不确定性：字段不足、关系不可信或指标不可执行时，系统说明缺口而不是补造结论。
3. 每个结果有证据：答案、图表、查询和写入都能追到来源、口径或运行回执。
4. 写入保持受控：读取、解释和预演不增加确认；导入提交、保存、覆盖和删除只确认一次真实影响。
5. 适应任意导入：生产空态不预置数据、图表或行业结论，也不默认选择销售、退款、渠道等特定字段。

## Product Wedge

稳定能力是“可信单图”：

```text
导入真实数据
  -> 查看证据摘要
  -> 描述一个图表或业务问题
  -> 必要时澄清一次字段
  -> 审阅图表、口径和证据
  -> 确认写入
```

“一次对话生成整套行业看板”保留为 Beta。它只能根据当前字段证据选择可执行单元，必须展示命中、缺失和省略项，不能把缺字段的组件渲染成确定结果。

## Product Boundary

- 本仓库是代码、文档和验证契约的边界，不是用户业务数据的边界。
- 数据库和业务文件保存在本地或用户配置的位置，不进入版本控制。
- API 和 UI 默认只监听回环地址；当前版本不支持远程公开服务。
- Agent 受当前工作区和白名单查询约束，不能接受任意 SQL，也不能绕过确认写入。
- 新工作区保持干净空态，无数据时只展示当前必要的导入路径。
- 可选模型不可用时，确定性本地能力仍需可用，并明确披露运行方式。

## Durable Experience Contract

- 一个业务能力只有一个主承接页：Sources 接入数据，Details 管理视图，Dashboards 创建图表，Evidence 核对证据，AI 审阅动作草案，System 管理工作区和本地配置。
- 其他页面只展示必要摘要并携带上下文跳转，不复制另一页面的完整操作。
- 第一屏只显示当前意图、确定结果、最小信任信息和下一步；维护工具按结构折叠。
- 普通概览不猜行业或指标；模糊图表请求不创建草案，先提供可点击业务字段。
- 详细交互、文案和确认规则由 `docs/product-ux-standard.md` 维护。

## Success Definition

- 空工作区没有样例、默认图表、默认答案或隐藏预置数据。
- 从导入到第一个可信图表不要求用户进入高级配置区。
- 单图请求最多进行一次必要澄清；真实写入只有一个确认面。
- 每个可见结论都有证据引用或明确阻塞原因。
- 不同屏幕比例下不出现全局横向溢出、文本裁切或控件重叠。
- 任意导入不会触发领域字段静默回退。

## Non-Goals

- 云端同步、多人实时协作、多租户隔离和企业级权限系统。
- 托管式数据仓库、通用 ETL 平台或无限制 SQL 控制台。
- 在仓库中保存真实业务数据、日志、凭据或用户备份。
- 用行业模板替代字段证据，或把 Beta 能力包装成稳定结果。
- 让 AI 取代确定性查询、证据回执和用户确认。
