# AIBI-C Product Positioning

## Positioning

AIBI-C 是面向本地业务导出表的证据型 AI BI 工作台。用户导入真实数据并描述问题，系统优先生成一个可追溯、可审阅、确认后才写入的图表或答案。

它不是固定行业模板库、SQL 管理工具或无证据聊天机器人。AI 负责缩短操作路径，确定性运行时负责导入、查询、证据和写入边界。

## Users And Core Job

主要用户是运营、财务、供应链、电商和 ERP 使用者。他们有 CSV 或 Excel 导出，知道业务问题，但不想先学习 SQL、数据建模和复杂看板编辑。

核心任务：从任意一份真实业务数据出发，用最少步骤得到第一个可信图表，并能回答数据来源、计算口径和未决问题。

数据实施与分析人员是次要用户。语义、指标、关系、公式、视图和布局能力保留，但不占据新手第一屏。

## Product Layers

| Layer | Promise | Release state |
| --- | --- | --- |
| 可信单图 | 导入数据，描述一个问题，核对证据，确认写入 | Stable，默认入口 |
| 可信连续分析 | 复用已确认语境与问法，导出证据，从已确认结果创建分析分支 | Stable advanced，按需展开 |
| 整套行业看板 | 根据当前字段证据组合一套看板，并披露命中、缺失和省略项 | Beta，不与单图入口竞争 |

## Primary Flow

`导入真实数据 -> 生成证据摘要 -> 描述一个图表 -> 核对证据 -> 确认写入`

- 字段明确时直接形成一个草案；不明确时最多澄清一次。
- 不确定或不可执行的结果不默认展示，也不补造行业结论。
- 只读、解释和预演不增加确认；真实写入只保留一个确认面。
- 每种业务对象只在一个页面维护，其他页面携带对象上下文跳转。

## Product Boundary

- 当前交付是单用户、本地优先的桌面工作台；API 与 UI 默认只监听回环地址。
- 新工作区保持空白，不预置数据、图表、问题、行业模板或隐藏默认值。
- 查询使用白名单参数，Agent 不能接受任意 SQL 或绕过确认写入。
- 用户数据、数据库、日志、凭据和备份不属于仓库内容。
- 可选模型不可用时，本地确定性导入、查询和证据能力仍需可用。
- 已验证的行业经验以版本化知识规则进入确定性查询边界，不依赖 Codex 会话或某个模型的隐式记忆。

## Experience Contract

- 第一屏只显示当前意图、确定结果、最小信任信息和下一步。
- Sources 接入数据，Details 管理视图，Dashboards 创建图表，Evidence 核对证据，AI 审阅写入，System 管理工作区与可信语境。
- 页面刷新、前进和后退必须恢复当前表、视图、看板、证据或动作，不使用样例对象兜底。
- 查询计划、原始回执、可信语境、确认问法和分析分支默认收起。
- 后续比较只能从已确认结果开始，并保留来源、查询与动作血缘。

## Success Criteria

- 空工作区没有样例或默认业务结论，无数据时只引导导入。
- 新用户不进入高级配置区也能完成第一个可信单图。
- 单图最多澄清一次，真实写入只确认一次。
- 每个可见结论都有证据或明确阻塞原因。
- 常见 PC 长宽比下无全局溢出、控件重叠和被迫换行。
- 任意导入不会触发领域字段静默回退。

## Non-Goals

- 云端同步、多人协作、多租户、企业权限与远程托管。
- 托管数据仓库、通用 ETL、任意 SQL 或通用代码执行器。
- 用行业模板替代字段证据，或把 Beta 包装成稳定结果。
- 让 AI 取代确定性查询、证据回执和用户确认。
