# AIBI-C 产品定位

## 产品承诺

AIBI-C 让持有 CSV、Excel 或受控数据源的业务用户，以最少步骤得到可追溯、可复算的答案或图表。用户不需要先学习 SQL 和建模，但始终可以核对来源、字段、口径、关系路径和阻塞原因。

它不是 SQL 管理器、固定行业模板库、通用 ETL、任意代码 Agent，也不是无证据聊天机器人。

## 目标用户与任务

- 业务使用者：导入真实数据，描述问题，得到第一个可信图表并继续分析。
- 结果使用者：核对证据、口径、可信度和缺口。
- 数据维护者：按需维护语义、指标、关系、公式、视图和看板，不干扰新手路径。

默认流程：`接入数据 -> 生成证据 -> 描述问题 -> 核对结果 -> 确认写入`。

## 能力层级

| 层级 | 用户承诺 | 产品级别 |
| --- | --- | --- |
| 可信单图 | 导入、提问、核对证据、确认保存 | 稳定默认入口 |
| 连续分析 | 从已确认结果创建有血缘的分析分支 | 稳定高级能力 |
| 语义与跨表 | 先消歧、声明粒度、验证关系，再受控执行 | 稳定受控能力 |
| 领域方案 | 显式启用 Domain Pack 后按字段证据组合单元 | Beta 高级入口 |

## 体验原则

- “工作区”只突出当前必要任务；主导航固定为工作区、数据、分析、看板、证据。
- 字段明确时直接形成结果或草案；歧义集中澄清一次，不能按列顺序或模型偏好静默决胜。
- 多表问题先展示统计粒度与关系路径；关系推荐不等于执行许可。
- 只读、解释和预演不增加确认；真实写入只有一个确认面。
- 不确定或不可执行时明确阻断，不补造数字或行业结论。

## 产品边界

- 单用户、本地运行、回环监听；不提供云同步、多租户或远程托管。
- 新工作区不预置数据、问题、图表、行业模板、Domain Pack 或隐藏默认值。
- Connector、Domain Pack 和看板单元相互独立，不能隐式启用彼此。
- Agent 不得执行任意 SQL、文件、网络、进程或写入操作；可选 Provider 不拥有工具权限。
- 用户数据、数据库、日志、凭据、备份和业务导出不属于仓库内容。
- 可信知识来自版本化规则与当前工作区证据，不来自会话隐式记忆。

## 成功标准

- 新用户无需展开高级建模即可完成第一个可信单图。
- 每个结论都有可复算证据或明确阻塞原因。
- 刷新、历史导航和跨页跳转保持真实对象上下文。
- 中性数据不会触发领域字段、缺口或模板的静默回退。
- 常见桌面与窄屏窗口无全局溢出、控件遮挡或核心入口丢失。

## 非目标

- 认证、角色、多人协作、原生移动客户端、云端数仓和远程灾备。
- 任意 SQL、任意代码、模型自主工具循环或隐藏写入。
- 用行业模板替代字段证据，或把 Beta 能力包装为稳定默认结果。

响应式 Web 在窄屏可用，但不等于提供原生移动产品。
