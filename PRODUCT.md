# AIBI-C 产品定位

## 定位

AIBI-C 是本地优先的证据型 AI BI 工作台。用户导入真实业务表并描述问题，系统给出可追溯、可审阅的答案或图表；确定性运行时负责计算和边界，AI 负责缩短操作路径。

它不是 SQL 管理器、固定行业模板库、通用 ETL、任意代码 Agent，也不是无证据聊天机器人。

## 用户与核心任务

主要用户是持有 CSV 或 Excel 等结构化导出的业务与数据使用者，包括但不限于运营、财务、供应链、电商、制造、教育、科研和项目管理。他们知道问题，但不希望先学习 SQL、建模和复杂看板编辑。

核心任务是：从一份真实数据出发，以最少步骤得到第一个可信图表，并能回答数据来源、计算口径、关系路径和未决问题。

数据实施人员可以按需维护语义、指标、关系、公式、视图和布局，但这些能力不占据新手第一屏。

## 产品层级

| 层级 | 用户承诺 | 当前定位 |
| --- | --- | --- |
| 可信单图 | 导入、提问、核对证据、确认写入 | 稳定默认入口 |
| 连续分析 | 复用已确认语境，从确认结果创建有血缘分支 | 稳定高级能力 |
| 语义与跨表 | 先消歧、声明粒度、验证关系，再受控执行 | 稳定受控能力 |
| 可选领域方案 | 显式启用领域包后，按字段证据组合单元并披露省略项 | Beta，高级入口 |

## 默认体验

`导入真实数据 -> 生成证据 -> 描述问题或图表 -> 核对结果 -> 确认写入`

- 字段明确时直接形成结果或草案；不明确时最多集中澄清一次。
- 同名、近义和跨表字段必须披露表级候选，不能靠列顺序或模型偏好静默决胜。
- 多表问题先展示统计粒度与关系路径；关系推荐不等于执行许可。
- 只读、解释和预演不增加确认；真实写入只有一个确认面。
- 不确定或不可执行的结果明确阻断，不补造行业结论。

## 产品边界

- 单用户、本地运行、回环监听；不提供云同步、多租户或远程托管。
- 新工作区不预置数据、问题、图表、行业模板或隐藏默认值。
- 新工作区不启用任何领域包；Connector、领域知识和看板单元彼此独立。
- 查询、Connector 和后台任务只使用白名单能力；Agent 不能执行任意 SQL、文件、网络或进程操作。
- 用户数据、数据库、日志、凭据、备份和业务导出不属于仓库内容。
- 可选模型不可用时，本地导入、查询、证据和受控写入仍可工作。
- 可信知识来自版本化规则和当前工作区证据，不来自会话隐式记忆。

## 成功标准

- 新用户无需展开高级建模即可完成第一个可信单图。
- 每个可见结论都有可复算证据或明确阻塞原因。
- 页面刷新、历史导航和跨页跳转保持真实对象上下文。
- 任意导入不会触发领域字段静默回退。
- 常见 PC 比例无全局溢出、控件重叠和正文逐字换行。

## 非目标

- 认证、角色、多人协作、移动端、云端数据仓库和远程灾备。
- 任意 SQL、任意代码执行、模型自主工具循环或隐藏写入。
- 用行业模板替代字段证据，或把 Beta 能力包装为稳定默认结果。
