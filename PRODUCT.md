# AIBI-C 产品定位

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 业务使用者：持有 CSV、Excel 或受控数据源，希望以最少步骤得到可信答案或图表，不需要先学习 SQL 和建模。
- 结果使用者：需要核对结论的来源、口径、可信度和缺口，再决定是否采用或继续分析。
- 数据维护者：按需维护语义、指标、关系、公式、视图和看板，同时不干扰新手完成主路径。

## Product Purpose

AIBI-C 让业务用户从真实数据出发完成“接入数据 → 生成证据 → 描述问题 → 核对结果 → 确认写入”，并为每个结论提供可追溯、可复算的证据或明确阻塞原因。

成功不是生成更多图表，而是让新用户无需展开高级建模就得到第一个可信单图，并能在刷新、历史导航和跨页跳转后继续核对同一个真实对象。

## Positioning

AIBI-C 将强分析能力放在渐进式工作流之后：字段明确时直接形成结果或草案；存在歧义时集中澄清；跨表问题先声明统计粒度和关系路径；任何真实写入都停在唯一确认面。相邻的 SQL 管理器、固定行业模板库或无证据聊天产品不能同时提供这套证据绑定与写入边界。

## Operating Context

- 本地单用户运行，服务只监听回环地址；工作区中的文件、数据库、证据和结果留在本机。
- 用户通常从现有 CSV、Excel、文件夹或显式配置的 Connector 开始，在工作区、数据、分析、看板和证据之间完成连续任务。
- 新工作区从空状态开始，不预置数据、问题、图表、行业模板、Domain Pack 或隐藏默认值。
- 默认流程面向业务语言；高级建模、关系和领域能力只在当前任务确实需要时渐进出现。

## Capabilities and Constraints

| 层级 | 用户承诺 | 产品级别 |
| --- | --- | --- |
| 可信单图 | 导入、提问、核对证据、确认保存 | 稳定默认入口 |
| 连续分析 | 从已确认结果创建有血缘的分析分支 | 稳定高级能力 |
| 语义与跨表 | 先消歧、声明粒度、验证关系，再受控执行 | 稳定受控能力 |
| 领域方案 | 显式启用 Domain Pack 后按字段证据组合单元 | Beta 高级入口 |

- Connector、Domain Pack 和看板单元相互独立，不能隐式启用彼此。
- Agent 不得执行任意 SQL、文件、网络、进程或写入操作；可选 Provider 不拥有工具权限。
- 可信知识来自版本化规则与当前工作区证据，不来自会话隐式记忆。
- 用户数据、数据库、日志、凭据、备份和业务导出不属于仓库内容。

## Brand Commitments

- 产品名保持为 AIBI-C；界面以克制、可信、可核对的工作台语气表达，不用营销夸张替代证据。
- 中文是主要工作语言，已存在的中英双语界面合同继续保持一致，不以缩写或技术术语增加理解成本。
- 视觉身份服务于高密度分析任务：强调阅读秩序、状态可信度和操作边界，而不是装饰性展示。

## Evidence on Hand

- 当前能力以仓库代码、`package.json`、本地运行时和 [实现状态](docs/implementation-status.md) 为准。
- 用户可观察行为由 [产品需求](docs/PRD.md)、[产品体验标准](docs/product-ux-standard.md) 和 [产品验收矩阵](docs/product-acceptance-matrix.md) 约束。
- 发布准备度必须由当次 `npm run preflight -- --stop-after` 及对应专项验证证明；历史绿色回执不能替代当前验证。
- 没有可用于产品界面的客户背书、市场份额、商业成绩或外部基准；未来界面不得虚构这些内容。

## Product Principles

1. **先给可信结果，再暴露复杂度。** 字段明确时直接推进；高级建模仅在必要处出现。
2. **证据先于表达。** 每个数字都能复算；不确定或不可执行时明确阻断，不补造行业结论。
3. **一次只做当前必要任务。** 工作区突出唯一主任务，次要工具渐进披露，避免让用户先学习系统结构。
4. **只读与写入边界清晰。** 解释和预演不增加确认；真实写入只有一个确认面，并允许拒绝。
5. **对象上下文连续。** 刷新、历史导航、跨页跳转和后续追问都保持真实工作区、来源、口径与血缘。

## Accessibility & Inclusion

- 常见桌面、横向 1280×720 和竖向 720×1280 必须保持完整、可读且无核心入口丢失。
- 当窗口短边小于 720px 时保持 720px 逻辑排版并整体等比缩小，不继续破坏信息层级。
- 关键路径必须支持键盘、可见焦点、语义化状态和 200% 缩放；状态不能只依赖颜色表达。
- 双语文本、长名称、动态数字和错误消息必须允许换行或截断，并保留可理解的恢复动作。

## Success Criteria

- 新用户无需展开高级建模即可完成第一个可信单图。
- 每个结论都有可复算证据或明确阻塞原因。
- 刷新、历史导航和跨页跳转保持真实对象上下文。
- 中性数据不会触发领域字段、缺口或模板的静默回退。
- 常见桌面与窄屏窗口无全局溢出、控件遮挡或核心入口丢失。

## Non-Goals

- 认证、角色、多人协作、原生移动客户端、云端数仓和远程灾备。
- 任意 SQL、任意代码、模型自主工具循环或隐藏写入。
- 用行业模板替代字段证据，或把 Beta 能力包装为稳定默认结果。
- 将响应式 Web 描述成原生移动产品。
